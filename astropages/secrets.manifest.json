{
  "version": 1,
  "integrations": [
    {
      "key": "calendly",
      "name": "Calendly",
      "secrets": [
        {
          "key": "CALENDLY_API_TOKEN",
          "label": "Calendly API token",
          "helpText": "Personal access token used server-side to read availability and create invitees.",
          "required": true,
          "environments": ["preview", "production"]
        },
        {
          "key": "CALENDLY_WEBHOOK_SIGNING_KEY",
          "label": "Calendly webhook signing key",
          "helpText": "Signing key used to verify Calendly webhook payloads.",
          "required": true,
          "environments": ["preview", "production"]
        }
      ]
    },
    {
      "key": "stripe",
      "name": "Stripe",
      "secrets": [
        {
          "key": "STRIPE_SECRET_KEY",
          "label": "Stripe secret key",
          "helpText": "Server-side Stripe key for Vera PaymentIntents, refunds and reconciliation.",
          "required": true,
          "environments": ["preview", "production"]
        },
        {
          "key": "STRIPE_WEBHOOK_SECRET",
          "label": "Stripe webhook secret",
          "helpText": "Environment-specific secret used to verify Stripe webhook signatures.",
          "required": true,
          "environments": ["preview", "production"]
        }
      ]
    },
    {
      "key": "ses",
      "name": "Amazon SES",
      "secrets": [
        {
          "key": "AWS_ACCESS_KEY_ID",
          "label": "SES access key ID",
          "helpText": "Least-privilege IAM access key used only for transactional email delivery.",
          "required": true,
          "environments": ["preview", "production"]
        },
        {
          "key": "AWS_SECRET_ACCESS_KEY",
          "label": "SES secret access key",
          "helpText": "Secret for the least-privilege SES IAM access key.",
          "required": true,
          "environments": ["preview", "production"]
        }
      ]
    },
    {
      "key": "posthog",
      "name": "PostHog",
      "secrets": [
        {
          "key": "POSTHOG_PERSONAL_API_KEY",
          "label": "PostHog personal API key",
          "helpText": "Optional provider-side key for project verification; never exposed to the generated site.",
          "required": false,
          "environments": ["preview", "production"]
        }
      ]
    },
    {
      "key": "google_places",
      "name": "Google Places",
      "secrets": [
        {
          "key": "GOOGLE_PLACES_API_KEY",
          "label": "Google Places API key",
          "helpText": "Platform-provided server-side key used to validate birth places and resolve time zones.",
          "required": true,
          "environments": ["preview", "production"]
        }
      ]
    }
  ]
}
